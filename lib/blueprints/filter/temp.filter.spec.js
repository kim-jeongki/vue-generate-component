import Vue from 'vue'
import {{name | camelCaseOnly}}Filter from './{{name | camelCaseOnly}}.filter.js'

// Here are some Jasmine 2.0 tests, though you can
// use any test runner / assertion library combo you prefer
describe('{{name | camelCaseOnly}}Filter', () => {
  // Inspect the filter instance on mount
  it('should correctly sets the message when created', () => {
    // const vm = new Vue({
    //   template: '<div>{{a}}</div>',
    //   data: { a: 'hello' }
    // }).$mount()
    // expect(vm.$el.innerHTML).toBe('HELLO')
  })
})